# Copy to .env and adjust. Used for local development.
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change-me-in-production

# Database (when added)
# DATABASE_URL=postgresql://user:pass@localhost:5432/t7e

# Redis (when RQ worker is used)
# REDIS_URL=redis://localhost:6379/0
