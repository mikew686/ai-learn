# ClusterIP for in-cluster access (pods use redis:6379). External access via redis-external (LoadBalancer) or port-forward.
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
