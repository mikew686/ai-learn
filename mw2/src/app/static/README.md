# Static assets

Served at `/static/` (Flask default). Use `url_for('static', filename='...')` in templates.

- **css/** – Stylesheets (e.g. `site.css` for overrides and custom CSS)
- **js/** – Scripts (e.g. `health-poll.js` for the health page)
- **images/** – Images, icons, favicons
