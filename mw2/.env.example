# Copy to .env and adjust. Used for local development.
# These match the defaults in the config package (Flask and RQ worker).

FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change-me-in-production

# Database (defaults = local Postgres after port-forward or LoadBalancer)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=localdev
# PGDATABASE=postgres
# Or set DATABASE_URL to override.

# Redis (default = local)
# REDIS_URL=redis://localhost:6379/0
